#!/bin/zsh

export STOW_FOLDERS="work,git,tmux,bin,nvim,fzf,alfred,ghostty"

pushd $HOME/.dotfiles
for folder in $(echo $STOW_FOLDERS | sed "s/,/ /g")
do
    stow --verbose --restow $folder
done
popd
