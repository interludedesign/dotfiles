#!/usr/bin/env bash
# Sourced by postgres utility scripts to validate and alias connection env vars

dot-env-check PGHOST PGPORT PGUSER PGPASSWORD PGDATABASE

host="$PGHOST"
port="$PGPORT"
user="$PGUSER"
password="$PGPASSWORD"
database="$PGDATABASE"
